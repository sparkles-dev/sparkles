plugins {
  id 'com.palantir.git-version' version '0.12.0-rc2'
  id 'com.adarshr.test-logger' version '1.5.0'
}

allprojects  {
  repositories {
    jcenter()
    mavenCentral()
  }
}

allprojects {
  version = gitVersion()
}
